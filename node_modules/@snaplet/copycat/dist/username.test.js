var import__ = require(".");
var import_testutils = require("./testutils");
const isUsername = (value) => Boolean(value.match(/[a-zA-Z0-9_\.]+/));
test("generating fake usernames", () => {
  (0, import_testutils.checkGeneratedValue)(isUsername, import__.copycat.fullName);
});
