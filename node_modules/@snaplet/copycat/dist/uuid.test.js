var import__ = require(".");
var import_testutils = require("./testutils");
var import_uuid = require("uuid");
test("generating fake usernames", () => {
  (0, import_testutils.checkGeneratedValue)(import_uuid.validate, import__.copycat.uuid);
});
