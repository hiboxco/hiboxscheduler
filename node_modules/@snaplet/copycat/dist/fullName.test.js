var import__ = require(".");
var import_testutils = require("./testutils");
const isNameSegment = (value) => value.length > 1 && value[0].toUpperCase() === value[0];
const isFullName = (value) => {
  const segments = value.split(" ");
  return segments.length > 1 && segments.every(isNameSegment);
};
test("generating fake first names", () => {
  (0, import_testutils.checkGeneratedValue)(isFullName, import__.copycat.fullName);
});
