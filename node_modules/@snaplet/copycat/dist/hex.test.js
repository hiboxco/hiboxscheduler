var import__ = require(".");
var import_testutils = require("./testutils");
const isHex = (value) => !Number.isNaN(parseInt(value, 16));
test("generating hex values", () => {
  (0, import_testutils.checkGeneratedValue)(isHex, import__.copycat.hex);
});
