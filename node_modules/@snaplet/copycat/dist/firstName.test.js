var import__ = require(".");
var import_testutils = require("./testutils");
const isFirstName = (value) => value.length > 1 && value[0].toUpperCase() === value[0];
test("generating fake first names", () => {
  (0, import_testutils.checkGeneratedValue)(isFirstName, import__.copycat.firstName);
});
