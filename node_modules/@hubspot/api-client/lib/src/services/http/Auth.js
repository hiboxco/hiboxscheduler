"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Auth = void 0;
const _ = require("lodash");
const AuthTypes_1 = require("./AuthTypes");
class Auth {
    static chooseAuth(opts = {}, config = {}) {
        let type;
        if (opts.authType) {
            if (opts.authType !== 'none' && _.get(config, opts.authType)) {
                type = opts.authType;
            }
        }
        else {
            _.forIn(AuthTypes_1.AuthTypes, (value, key) => {
                if (_.get(config, key)) {
                    type = key;
                }
            });
        }
        return type;
    }
}
exports.Auth = Auth;
//# sourceMappingURL=Auth.js.map