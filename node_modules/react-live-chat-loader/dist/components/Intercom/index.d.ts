/// <reference types="react" />
interface Props {
    color?: string;
}
declare const Intercom: {
    ({ color }: Props): JSX.Element | null;
    defaultProps: {
        color: string;
    };
};
export default Intercom;
