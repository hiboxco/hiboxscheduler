import { BitBuilderBase } from '../internal/base';
import { Confirm, Danger, Deny, End, Primary, Text, Title } from '../internal/methods';
export interface ConfirmationDialogParams {
    confirm?: string;
    deny?: string;
    text?: string;
    title?: string;
}
export interface ConfirmationDialogBuilder extends Confirm<string>, Danger, Deny, End, Primary, Text, Title {
}
/**
 * @@link https://api.slack.com/reference/block-kit/composition-objects#confirm
 * @@displayName Confirmation Dialog
 */
export declare class ConfirmationDialogBuilder extends BitBuilderBase {
}
