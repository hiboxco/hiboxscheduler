import { AsnData } from "./asn_data";
export declare type AsnExportType = "hex" | "base64" | "base64url" | "pem";
export declare type AsnEncodedType = BufferSource | string;
export declare abstract class PemData<T> extends AsnData<T> {
    static isAsnEncoded(data: any): data is AsnEncodedType;
    static toArrayBuffer(raw: BufferSource | string): ArrayBuffer;
    protected abstract readonly tag: string;
    constructor(raw: AsnEncodedType, type: {
        new (): T;
    });
    constructor(asn: T);
    toString(): string;
    toString(format: "hex" | "base64" | "base64url" | "pem"): string;
}
