import { n as nodeHTTPRequestHandler } from '../../../dist/nodeHTTPRequestHandler-372a4a4f.esm.js';
import 'url';
import '../../../dist/resolveHTTPResponse-ddb56677.esm.js';
import '../../../dist/transformTRPCResponse-45c225c3.esm.js';
import '../../../dist/codes-5678cc97.esm.js';

/* eslint-disable @typescript-eslint/no-explicit-any */
function createExpressMiddleware(opts) {
  return async (req, res) => {
    const endpoint = req.path.slice(1);
    await nodeHTTPRequestHandler({ ...opts,
      req,
      res,
      path: endpoint
    });
  };
}

export { createExpressMiddleware };
