export declare const signature: {
    EXPECTED_SCHEME: string;
    _computeSignature: (payload: string, secret: string) => string;
    verifyHeader(payload: string, header: string, secret: string, tolerance?: number): boolean;
};
/**
 * Generates a header to be used for webhook mocking
 */
interface Options {
    timestamp?: number;
    payload: string;
    secret: string;
    scheme?: string;
    signature?: string;
}
export declare function generateTestHeaderString(opts: Options): string;
export {};
